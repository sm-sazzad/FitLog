import { IData } from '@/lib/Type';
import React from 'react';

const SelectedCard = ({ cardData }: { cardData: IData }) => {
    return (
        <div>

        </div>
    );
};

export default SelectedCard;